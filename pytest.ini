[pytest]
testpaths = tests
markers =
    browser: drives a real browser via Playwright (skip with -m "not browser")
    postgres: runs against a real PostgreSQL (needs POSTGRES_TEST_URL; select with -m postgres)
    real_favicon_backfill: lets the post-import favicon thread actually run
filterwarnings =
    ignore::DeprecationWarning:urllib3
